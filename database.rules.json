{
  "rules": {
    "rooms": {
      "$roomId": {
        ".read": "auth != null",
        "status": {
          ".write": "auth != null" 
        },

        "users": {
          "$uid": {
            ".write": "auth != null && auth.uid === $uid"
          }
        }
      }
    }
  }
}